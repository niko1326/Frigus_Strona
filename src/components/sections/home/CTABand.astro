---
import CTAButton from '../../CTAButton.astro';
import { PRIMARY_PHONE } from '../../../config/site';
import type { Locale } from '../../../i18n/content';

interface Props {
  lang: Locale;
}

const { lang } = Astro.props;
const isPL = lang === 'pl';
---

<section class="section" data-reveal>
  <div class="container cta-inline">
    <p class="plain-highlight plain-highlight-sm">{isPL ? 'Zadzwoń i umów wycenę' : 'Call and schedule your quote'}</p>
    <CTAButton
      href={PRIMARY_PHONE.href}
      label={isPL ? 'Zadzwoń po wycenę' : 'Call for a quote'}
      ariaLabel={isPL ? 'Zadzwoń po wycenę' : 'Call for a quote'}
    />
  </div>
</section>
